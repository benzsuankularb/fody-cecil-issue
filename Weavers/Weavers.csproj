<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="FodyCecil" Version="3.0.1" />
    <PackageReference Include="FodyHelpers" Version="3.0.3" />
  </ItemGroup>

  <!-- <Target Name="Repack" AfterTargets="Build">
    <ItemGroup>
      <LibraryPath Include="$(NuGetPackageRoot)\netstandard.library\2.0.0\build\netstandard2.0\ref" />
    </ItemGroup>
    <ItemGroup>
      <InputAssemblies Include="$(OutputPath)\NUNET.dll" />
    </ItemGroup>
    <ILRepack LibraryPath="@(LibraryPath)" Parallel="true" Internalize="true" InputAssemblies="@(InputAssemblies)" TargetKind="Dll" OutputFile="$(OutputPath)\$(AssemblyName).dll" />
  </Target> -->
  
  <!-- <Target Name="Copy" AfterTargets="Build">
    <Copy SourceFiles="$(TargetPath)" DestinationFolder="$(SolutionDir)\..\stupid-circus-core\Weavers\bin" />
  </Target> -->

</Project>
